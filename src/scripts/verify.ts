import { run } from './helloWorld';

// state.set(
//     key,
//     text.replace(/\$\{([^\}]+)\}/g, (full, prop) => {
//         if(state.has(prop)) {
//             return state.get(prop);
//         } else {
//             return 'undefined';
//         }
//     })
// );
